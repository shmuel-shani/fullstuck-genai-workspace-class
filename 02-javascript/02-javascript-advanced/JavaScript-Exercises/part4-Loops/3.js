const N = 7;
let sum = 0; // מתחילים קופה מ-0 (כי זה חיבור)

for (let i = 1; i <= N; i++) {
    // שואלים את המחשב: האם המספר הנוכחי מתחלק ב-3?
    if (i % 3 === 0) {
        sum = sum + i; // מוסיפים לקופה
    }
}

console.log("The sum is: " + sum); // יודפס 9

