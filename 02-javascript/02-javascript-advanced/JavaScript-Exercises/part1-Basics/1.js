// 1. קליטת 3 מספרים (לצורך הדוגמה, ניתן להם ערכים קבועים)
const num1 = 10;
const num2 = 20;
const num3 = 30;

// 2. חישוב הסכום
const sum = num1 + num2 + num3;

// 3. חישוב הממוצע
const average = sum / 3;

// 4. הצגה למסך
console.log("The sum is: " + sum);           // מדפיס 60
console.log("The average is: " + average);   // מדפיס 20



