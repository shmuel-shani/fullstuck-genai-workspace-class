const width = 5;
const height = 4;

// חישוב שטח (כפל)
const area = width * height;

// חישוב היקף (סוגריים קודמים לכפל!)
const perimeter = 2 * (width + height);

console.log("Area: " + area);           // מדפיס 20
console.log("Perimeter: " + perimeter); // מדפיס 18


